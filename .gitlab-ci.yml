variables:
  MONGODB_URL: "mongo:27017/task-k8s"

build-job:
  image: node:alpine
  stage: build
  script:
    - npm install

test-job1:
  image: node:alpine
  services:
    - mongo
  stage: test
  before_script:
    - npm install
  script:
    - npm test